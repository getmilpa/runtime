parameters:
    level: 6
    paths:
        - src
    # Value objects such as Stack\ServiceDeclaration are built by FOREIGN plugins: a `list<PortMapping>`
    # in a docblock is a promise the constructor verifies at runtime and refuses loudly when broken.
    # PHPDoc types are hints at that boundary, not certainties.
    treatPhpDocTypesAsCertain: false
